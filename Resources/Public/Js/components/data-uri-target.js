// Data URI Target Komponente
// Data URI Target Elemente koennen Block-Level Elemente wie Ueberschriften aufnehmen, damit diese aber trotzdem komplett
// verlinkt werden koennen wird der Ziellink aus einem Data-Attribute ausgewertet
(function () {
	'use strict';

	// xna.on('documentLoaded', function() {
	// 	document.querySelectorAll('[data-uri-target]').forEach(function(node) {
	// 		node.addEventListener('click', function(event) {
	// 			document.location.href = node.dataset.uriTarget;
	// 			event.preventDefault();
	// 		});
	// 	});
	// });
})();